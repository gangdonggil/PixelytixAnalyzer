{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "make",  // Makefile을 호출하여 빌드 실행
      "args": [],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"],
      "detail": "Makefile을 통한 빌드"
    },
    {
      "label": "clean",
      "type": "shell",
      "command": "make",
      "args": ["clean"],  // Makefile의 clean 타겟 실행
      "group": "none"  // clean 작업은 group 설정 필요 없음
    }
  ]
}
